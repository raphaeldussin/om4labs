language: python
sudo: false
notifications:
  email: false

install:
  - pip install virtualenv
  - virtualenv testing
  - source testing/bin/activate
  - pip install -r ci/requirements.txt
  - pip install -e .

script:
  - py.test om4labs
  - black --check om4labs
